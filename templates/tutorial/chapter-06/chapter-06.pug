extends ../tutorial.pug

block title
  | Chapter 6: Route Groups - F4 Framework Tutorial

block content
  .chapter-header
    .chapter-number Chapter 6
    h1 Route Groups
    p.description Organize and scale your application with route groups

  .content-section
    h2 Introduction

    p As applications grow, managing hundreds of individual routes becomes unwieldy. Route groups solve this by letting you organize related routes, share configuration, and apply middleware to multiple routes at once.

    h3 What We'll Cover

    ul
      li Creating route groups with path prefixes
      li Sharing middleware across routes
      li Building RESTful APIs
      li Nesting route groups
      li API versioning strategies
      li Organizing large applications

  .content-section
    h2 What Are Route Groups?

    p A <code>RouteGroup</code> is a container that:

    ul
      li Groups related routes under a common path prefix
      li Applies shared middleware to all routes
      li Organizes routes logically
      li Makes APIs easier to maintain

    .info-box
      strong Key Concept:
      | Route groups don't just prefix paths—they create a complete routing context with shared behavior.

  .content-section
    h2 Creating Your First Route Group

    h3 Basic Syntax

    pre
      code.language-php.
        use F4\Core\{Route, RouteGroup};

        $group = new RouteGroup('/api');

        $group->addRoute(Route::get('/users', function() {
            return ['users' => [/* ... */]];
        }));

        $group->addRoute(Route::get('/posts', function() {
            return ['posts' => [/* ... */]];
        }));

        $f4->addRouteGroup($group);

        // Creates routes: /api/users and /api/posts

    h3 Alternative: fromRoutes()

    pre
      code.language-php.
        $group = RouteGroup::fromRoutes(
            Route::get('/users', function() { /* ... */ }),
            Route::get('/posts', function() { /* ... */ })
        )->setPathPrefix('/api');

        $f4->addRouteGroup($group);

  .content-section
    h2 Path Prefixes

    p The group prefix is prepended to all route paths:

    table
      thead
        tr
          th Group Prefix
          th Route Path
          th Final URL
      tbody
        tr
          td
            code /api
          td
            code /users
          td
            code /api/users
        tr
          td
            code /api/v1
          td
            code /products
          td
            code /api/v1/products
        tr
          td
            code /admin
          td
            code /dashboard
          td
            code /admin/dashboard

  .content-section
    h2 Shared Middleware

    p Apply middleware to all routes in the group:

    pre
      code.language-php.
        $apiGroup = new RouteGroup('/api')
            ->before(function($request, $response, $route) {
                // Validate API key for ALL /api/* routes
                $apiKey = $request->getHeader('X-API-Key');
                if (!validateApiKey($apiKey)) {
                    throw new HttpException('Invalid API key', 403);
                }
                return $request;
            })
            ->after(function($response, $request, $route) {
                // Add CORS headers to all API responses
                return $response
                    ->withHeader('Access-Control-Allow-Origin', '*');
            });

  .content-section
    h2 RESTful API Example

    p Route groups are perfect for RESTful APIs:

    pre
      code.language-php.
        use F4\Core\{Route, RouteGroup};

        $usersApi = new RouteGroup('/api/users')
            ->addRoute(Route::get('', function() {
                // GET /api/users - List all users
                return ['users' => User::all()];
            }))
            ->addRoute(Route::post('', function($name, $email) {
                // POST /api/users - Create new user
                $user = User::create($name, $email);
                return ['user' => $user];
            }))
            ->addRoute(Route::get('/{id:int}', function(int $id) {
                // GET /api/users/123 - Get specific user
                return ['user' => User::find($id)];
            }))
            ->addRoute(Route::put('/{id:int}', function(int $id, $name, $email) {
                // PUT /api/users/123 - Update user
                $user = User::update($id, $name, $email);
                return ['user' => $user];
            }))
            ->addRoute(Route::delete('/{id:int}', function(int $id) {
                // DELETE /api/users/123 - Delete user
                User::delete($id);
                return ['success' => true];
            }));

        $f4->addRouteGroup($usersApi);

  .content-section
    h2 RESTful Resource Pattern

    table
      thead
        tr
          th HTTP Method
          th Path
          th Action
          th Purpose
      tbody
        tr
          td GET
          td
            code /api/users
          td Index
          td List all users
        tr
          td GET
          td
            code /api/users/{id}
          td Show
          td Get single user
        tr
          td POST
          td
            code /api/users
          td Create
          td Create new user
        tr
          td PUT
          td
            code /api/users/{id}
          td Update
          td Update existing user
        tr
          td DELETE
          td
            code /api/users/{id}
          td Delete
          td Delete user

  .content-section
    h2 API Versioning

    p Use route groups to manage multiple API versions:

    h3 Version 1 API

    pre
      code.language-php.
        $v1 = new RouteGroup('/api/v1')
            ->addRoute(Route::get('/users', function() {
                // Old response format
                return ['data' => User::all()];
            }));

        $f4->addRouteGroup($v1);

    h3 Version 2 API

    pre
      code.language-php.
        $v2 = new RouteGroup('/api/v2')
            ->addRoute(Route::get('/users', function() {
                // New response format with pagination
                return [
                    'data' => User::paginate(),
                    'meta' => ['version' => '2.0']
                ];
            }));

        $f4->addRouteGroup($v2);

    p Both versions coexist:
    ul
      li
        code /api/v1/users
        |  - Old format (maintained for backwards compatibility)
      li
        code /api/v2/users
        |  - New format (latest features)

  .content-section
    h2 Complete Application Structure

    p Here's how to organize a full application:

    pre
      code.language-php.
        use F4\Core\{Route, RouteGroup};

        // Public routes (no auth)
        $f4->addRoute(Route::get('/', function() {
            return ['page' => 'home'];
        }));

        $f4->addRoute(Route::get('/about', function() {
            return ['page' => 'about'];
        }));

        // Public API
        $publicApi = new RouteGroup('/api/public')
            ->after(corsHeaders())
            ->addRoute(Route::get('/posts', function() { /* ... */ }))
            ->addRoute(Route::get('/products', function() { /* ... */ }));

        // Protected API (requires authentication)
        $authApi = new RouteGroup('/api/auth')
            ->before(requireAuth())
            ->after(corsHeaders())
            ->addRoute(Route::get('/profile', function() { /* ... */ }))
            ->addRoute(Route::post('/orders', function() { /* ... */ }));

        // Admin panel
        $adminPanel = new RouteGroup('/admin')
            ->before(requireAdmin())
            ->addRoute(Route::get('/dashboard', function() { /* ... */ }))
            ->addRoute(Route::get('/users', function() { /* ... */ }))
            ->addRoute(Route::get('/settings', function() { /* ... */ }));

        // Register all groups
        $f4->addRouteGroup($publicApi);
        $f4->addRouteGroup($authApi);
        $f4->addRouteGroup($adminPanel);

  .content-section
    h2 Module Organization

    p Split large applications into modules, each with its own route group:

    pre
      code.language-php.
        // src/App/Modules/UserModule.php
        namespace App\Modules;

        class UserModule implements ModuleInterface
        {
            public function __construct(CoreApiInterface &$f4)
            {
                $group = new RouteGroup('/users')
                    ->addRoute(Route::get('', [UserController::class, 'index']))
                    ->addRoute(Route::get('/{id:int}', [UserController::class, 'show']));

                $f4->addRouteGroup($group);
            }
        }

  .content-section
    h2 Benefits of Route Groups

    table
      thead
        tr
          th Benefit
          th Description
      tbody
        tr
          td Organization
          td Group related routes together logically
        tr
          td DRY Code
          td Share middleware instead of repeating it
        tr
          td Maintainability
          td Easy to find and modify related routes
        tr
          td Scalability
          td Add new features without cluttering main routing file
        tr
          td Versioning
          td Support multiple API versions simultaneously
        tr
          td Security
          td Apply authentication to entire sections at once

  .content-section
    h2 Best Practices

    ol
      li Use meaningful group names that reflect their purpose
      li Keep groups focused (users, posts, admin, api)
      li Apply authentication middleware at the group level
      li Version your APIs from the start (<code>/api/v1</code>)
      li Organize large apps into modules
      li Document group middleware behavior
      li Nest groups for complex hierarchies
      li Use consistent naming conventions

  .content-section
    h2 Try It Yourself

    p Practice exercises:

    ol
      li Create a RESTful API for a blog (posts resource)
      li Build an admin panel with nested route groups
      li Implement API versioning (v1 and v2)
      li Organize routes into modules (Auth, Shop, Blog)
      li Add shared middleware for logging and CORS

  .content-section
    h2 Key Takeaways

    ul
      li Route groups organize related routes with path prefixes
      li Middleware can be shared across all routes in a group
      li Groups support nesting for complex applications
      li Perfect for building RESTful APIs
      li Essential for API versioning
      li Improves code organization and maintainability
      li Modules should use route groups for their routes

  .content-section
    h2 What's Next?

    p In Chapter 7, we'll explore database integration:

    ul
      li F4's query builder
      li SELECT, INSERT, UPDATE, DELETE operations
      li JOINs and relationships
      li Transactions
      li Error handling
      li Best practices for data access

    p Ready to work with databases? Click "Next" below!
